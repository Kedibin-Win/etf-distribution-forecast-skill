# 回测报告

> 项目：AI量化实盘（deepredict） · 数据源：Tushare Pro
> 回测日期：2026-09-23 · 训练轮数：300（AMP 混合精度，RTX 3070 Laptop 8GB）
> 测试集区间：**2024-12-09 ~ 2026-06-24**（372 个交易日）
> 口径说明：策略指标取自**真实资金曲线**（`组合价值` 列，含交易成本与调仓限制）

---

## 一、核心结论（先看这个）

| 指标 | 策略 | 买入持有（等权 63 只 ETF） | 对比 |
|---|---|---|---|
| 累计收益 | **+23.12%** | +31.86% | ❌ **跑输 8.74 pp** |
| 最大回撤 | **9.34%** | 13.10% | ✅ **回撤小 3.76 pp** |
| 夏普比率 | **1.44** | 1.05 | ⚠️ 接近 |
| 平均仓位 | 33.6% | 100% | — |
| 空仓天数 | 191 / 372 | 0 | — |
| 交易成本 / 次数 | ¥4,354.26 / 138 次 | 0 | — |

**一句话结论**：

> **工程链路完全跑通，回测结果真实可信；但模型在样本外没有跑赢基准。**
> 策略用 **33.6% 的平均仓位** 取得 +23.12% 收益，把最大回撤从 13.10% 压到 9.34%，**代价是错过了上涨行情的主要涨幅**。
> 因此它更像一个**降波动的择时器**，而不是**alpha 来源**。

> 备注：`portfolio_stats.txt` 中的「最大回撤 8.41% / 夏普 1.11」由近似公式
> （`日收益 × 仓位` 复利，**未计交易成本**）得出；上表使用的是真实资金曲线，更保守、更可信。

---

## 二、模型预测能力评估（诚实版）

| 指标 | 数值 | 判读 |
|---|---|---|
| 预测均值 vs 真实均值 相关性 | **0.0721** | 🔴 **极弱**（接近无预测能力） |
| 涨跌方向命中率 | **56.7%** | 🔴 接近随机（50%） |
| 预测均值区间 | -0.9839% ~ 1.2250% | 量纲正确 |
| 真实均值区间 | -8.3227% ~ 4.2561% | 量纲正确 |

**说明**：预测值量纲已正确（±1% 级别，与真实日收益同量级），说明数据与模型链路是干净的；
但**预测精度不足**——相关性 0.0721 意味着策略收益主要来自 **beta 暴露 + 波动控制**，而非择时 alpha。

### 训练过程诊断

| Epoch | Train Loss | Val Loss |
|---|---|---|
| 50 | 0.6001 | 1.5334 |
| 100 | 0.5267 | **1.4862** ← 最优 |
| 150 | 0.4698 | 1.4867 |
| 200 | 0.4286 | 1.5133 |
| 250 | 0.4020 | 1.5507 |
| 300 | 0.3937 | 1.5646 |

> 🔴 **明显过拟合**：Train Loss 持续下降，但 **Val Loss 在第 100 轮触底后单调回升**。
> 300 轮并非最优，实际最优约在 100~150 轮。项目自带早停（patience=200）在 300 轮内未触发，建议：
> - 缩短训练轮数至 ~150，或
> - 增大 dropout / weight_decay，或
> - 缩小早停 patience

---

## 三、黑天鹅漂移闸门表现

| 指标 | 数值 |
|---|---|
| 漂移超阈值天数 | 43 / 372（11.6%） |
| 漂移均值 | 0.2213 |
| 阈值均值 | 0.2477 |
| 漂移−阈值 平均差 | -0.0265（负值=多数时间未触发） |

> 闸门在 11.6% 的交易日生效，平均仓位被压到 33.6% —— **闸门偏保守**，
> 这是策略跑输基准的直接原因（上涨行情中仓位不足）。

### 仓位分布

| 仓位 | 天数 |
|---|---|
| 0.0 | 191 |
| 0.1 | 5 |
| 0.2 | 6 |
| 0.3 | 40 |
| 0.4 | 8 |
| 0.5 | 8 |
| 0.6 | 16 |
| 0.7 | 9 |
| 0.8 | 1 |
| 0.9 | 7 |
| 1.0 | 81 |

---

## 四、本次回测中修复的 4 个关键缺陷

> ⚠️ 前两次回测曾出现 **+12797% / +20065%** 的荒谬结果与 **-627% 的"日收益"**，
> 根因是多个缺陷叠加。修复后才得到上面的可信结果。

### 缺陷 1：Tushare ETF 前复权未生效（数据层）

- **现象**：`510230.SH` 2020-08-17 出现 -78.8%、`159928.SZ` 2021-06-25 出现 -74.5%、`516160.SH` 2024-09-18 出现 +221%
- **根因**：`ts.pro_bar(asset='FD', adj='qfq')` 对 ETF **不做复权**（输出与不复权完全一致）；
  上述日期实为 **ETF 份额拆分/合并**（`fund_adj` 因子从 1.0 跳到 4.0 等）
- **修复**：改用 `fund_daily`（原始价）+ `fund_adj`（复权因子）手工换算前复权
- **效果**：`close_return` 极值 2.2133 → **0.2004**（= 20% 涨跌停上限，合理）

### 缺陷 2：股票池顺序被打乱（可复现性）

- **现象**：`train_data_generator._get_stock_pool` 用 `list(set(symbols))` 去重
- **根因**：字符串 `set` 顺序受 `PYTHONHASHSEED` 影响，**每次运行顺序可能不同**；
  而特征列顺序 = 股票池顺序，错位会导致 `predict.py` 的标的名称映射整体错配，且结果不可复现
- **修复**：改为保序去重 `list(dict.fromkeys(symbols))`

### 缺陷 3：`main.py` 跨步索引 + 二次标准化（最关键）

这是**原项目代码中既有的缺陷**，也是导致回测结果荒谬的主因：

| 子问题 | 位置 | 说明 |
|---|---|---|
| 跨步索引步长错误 | `main.py` L540–541 | `extract_feature(data, 3, num_symbols)` 中第三参数是**跨步长度**，数据布局为 `[sym0_f0..f19, sym1_f0..f19, ...]`，步长必须是 **NUM_FEATURES(=20)**，原代码传 `num_symbols(=63)` 会抽出 20 列**互不相关的随机特征**，使"真实收益/预测收益/相关性/回测绩效"全部失真 |
| 二次标准化 | `main.py` L~350 | `train_data_generator` 保存 `training_data.npy` 时**已标准化**并另存了 `data_mean.npy`/`data_std.npy`，但 `main.py` 又调用 `normalize_data()` 标准化了一次。反标准化后得到的是 **z-score 而非收益率**，回测把 z-score 当日收益使用（出现 -627%） |

**修复**：
1. `extract_feature` 调用改传 `NUM_FEATURES`
2. 新增 `load_norm_params()`，真实数据路径**复用生成器的标准化参数，跳过二次标准化**（模拟数据路径保留原行为）

### 缺陷 4：`组合价值` 列整体滞后一日（报表层）

- **现象**：`black_swan_analysis.csv` 的 `组合价值` 末值为 ¥1,221,937.32（+22.19%），
  与 `portfolio_stats.txt` 的最终资金 ¥1,231,215.92（+23.12%）**对不上**
- **根因**：`portfolio_values` 初始就含 1 个元素（初始资金），此后每日追加 1 个，共 `len(true_means)+1` 个；
  原代码 `portfolio_values[:len(true_means)]` **丢掉了最后一天**，导致整列相对日期滞后一日
- **验证**：`csv[-1]/csv[-2] = 0.9776592` 恰好等于 `1 + 真实均值[-2] × 仓位[-2]`，
  证明该列存的值对应的是**前一天**的收盘资金
- **修复**：`main.py` L777 改为 `portfolio_values[1:len(true_means)+1]`；本次已同步修补 CSV（左移一位并补回真实终值）

**效果对比（修复前 → 修复后）**：

| 指标 | 修复前 | 修复后 |
|---|---|---|
| 真实均值最大值 | +166% | **+4.2561%** |
| 真实均值最小值 | -627% | **-8.3227%** |
| 总收益率 | +20065% | **+23.12%** |
| 最大回撤 | 98.77% | **9.34%** |
| 组合价值口径 | 滞后一日 | **与日期对齐** |

---

## 五、待改进项（按优先级）

| 优先级 | 问题 | 建议 |
|---|---|---|
| 🔴 高 | 模型预测能力弱（相关性 0.0721） | 增加特征、调整模型容量、或改为分类任务（涨/跌） |
| 🔴 高 | 过拟合（Val Loss 100 轮后回升） | 训练轮数降至 ~150；或加强正则化 |
| 🔴 高 | 漂移闸门过保守（平均仓位仅 33.6%） | 下调漂移阈值分位（当前近 20 日 90 分位）或增加滞回 |
| 🟡 中 | 无基准对比功能 | 建议把 `BacktestConfig.compare_indices` 真正接上（当前是死配置） |
| 🟡 中 | 回测无滑点/涨跌停/停牌处理 | 加入滑点模型与涨跌停过滤 |
| 🟡 中 | `portfolio_stats` 用近似公式算回撤/夏普（未计成本） | 统一改用真实资金曲线口径（本报告已采用） |
| 🟡 中 | `volume_change` 极端值（最大 790 倍） | 上市初期流动性枯竭所致（真实现象），建议 winsorize 或对数化 |
| 🟢 低 | 回测内嵌于训练脚本，不能单独回测 | 抽出独立回测脚本，复用已训练模型 |

---

## 六、复现方式

```bash
# 1. 生成数据（Tushare 数据源，含前复权）
uv run python train_data_generator.py

# 2. 训练 + 回测（无界面环境需 MPLBACKEND=Agg）
MPLBACKEND=Agg uv run python main.py --epochs 300 --batch-size 128 --amp

# 3. 查看结果
cat predict_output/portfolio_stats.txt
#   predict_output/black_swan_analysis.csv    ← 逐日明细（含仓位/净值/漂移）
#   predict_output/回测净值对比图.png          ← 净值/仓位/漂移三联图
```

**本次运行环境**：Windows / Python 3.10.6 / tushare 1.4.29 / RTX 3070 Laptop 8.59GB
**耗时**：数据生成约 2 分钟，训练+回测约 6.5 分钟

---

*报告由回测日志与 `black_swan_analysis.csv` 自动汇总生成，2026-09-23。*

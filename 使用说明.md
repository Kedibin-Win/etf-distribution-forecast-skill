# AI量化实盘 — 项目使用说明

> 项目代号：**deepredict**（pyproject.toml）
> 类型：ETF 全市场分布预测 + 仓位控制 + 黑天鹅预警 + 期权对冲分析
> 运行平台：Windows（依赖 QMT / xtquant 数据源，可用本地 CSV 兜底）

---

## 一、这个项目是干什么的

一句话概括：**用 Transformer 预测"下一个交易日整个市场的收益分布"，而不是预测单只标的的涨跌**；再基于这个分布算出风险指标（VaR、偏度、峰度）、交易信号和仓位，并用分布漂移（JS 散度）作为黑天鹅闸门。

核心思路链条：

```
QMT 拉取 63 只 ETF 的 OHLCV
        ↓
加工成 20 维技术特征（每只 ETF）
        ↓
拼成高维矩阵：[时间点 × (63 只 × 20 特征)]
        ↓
Transformer 编码器（窗口 60 天 → 预测下一天全部 1260 维输出）
        ↓
对输出中的 close_return 做横截面统计
        ↓
① 全市场均值 / VaR5% / 偏度峰度  → 风险评估（0-100 分）
② 分布漂移 JS 散度              → 黑天鹅预警
③ 均值 + 漂移阈值               → 仓位档位（0 / 50% / 100%）
        ↓
回测绩效（收益率 / 夏普 / 最大回撤）+ 可视化热力图
```

关键设计理念：**预测"分布"而非"点位"**。因此系统对"全市场普跌""尾部风险加剧""市场分化"这类系统性状态敏感，更适合做仓位择时与风险控制，而不是选个股。

---

## 二、目录结构

```
AI量化实盘/
├── config.py                  # 【总配置】数据 / 股票池 / 模型 / 回测 / QMT 连接
├── pyproject.toml             # uv 依赖声明（Python ≥ 3.13）
├── train_data_generator.py    # ① 训练数据生成（拉数据 + 算特征 + 存 .npy）
├── main.py                    # ② 模型训练 + 回测（Transformer）
├── predict.py                 # ③ 单日预测（风险评分 + 交易信号）
├── etf_hedge_analysis.py      # ④ ETF 期权对冲分析（行业 vs 宽基相关性）
├── visualize.py               # ⑤ Streamlit 可视化（热力图 + 黑天鹅图）
└── README.md                  # 本文件
```

**运行后会生成（当前不存在，需自行生成）：**

```
data/            training_data.npy / training_dates.npy / data_mean.npy / data_std.npy
models/          distribution_predictor.pth / config.npy
predict_output/  prediction_YYYY-MM-DD.csv / summary_YYYY-MM-DD.txt / black_swan_analysis.csv / portfolio_stats.txt
hedge_data/      hedge_results.csv / correlation_matrix.csv / option_info.csv / etf_returns.csv
cache/           market_data/  hedge_data/   （pickle 缓存，加速二次拉取）
ETF.json         （标的中文名映射，需自备；缺失时回落显示代码）
```

---

## 三、各文件功能详解

### 1. `config.py` — 全局配置中枢

所有脚本都从这里读参数，**改配置只改这个文件**。

| 配置类 | 作用 | 关键参数 |
|---|---|---|
| `DataConfig` | 数据与股票池 | `start_date=2016-01-01`、`end_date=2026-06-24`、`input_window=60`；`stock_pool.type='etf'`，内置 **63 只 ETF 清单** |
| `FactorConfig` | 因子加工 | `batch_size=1000`、`max_workers=4` |
| `ScorecardConfig` | 评分卡建模（备用方案） | `test_size=0.3`、`C=100`、`pdo=50`、`score_0=600` |
| `BacktestConfig` | 回测 | `initial_capital=100 万`、`top_ratio=1%`、`transaction_cost=0.0001` |
| `DataStorageConfig` | 数据/输出目录 | `data/`、`output/` |
| `QMTConfig` | **QMT 数据中心连接** | `token`（空，需填）、`data_home_dir=C:\qmtdata`、`addr_list` |
| `QMTTraderConfig` | **QMT 交易端** | `path=C:\你的券商QMT交易端\userdata_mini`、`account_id` |

`stock_pool.type` 四选一：`sector`（板块成分股）/ `custom`（自定义）/ `etf`（内置 ETF 列表）/ `csv`（从 CSV 读）。

---

### 2. `train_data_generator.py` — 训练数据生成器

**职责**：把原始行情变成模型能吃的标准化 `.npy` 文件。

- **数据来源**：优先 QMT（`xtdata`）；不可用时自动降级读本地 `data/etf_hist_data.csv`。
- **缓存机制**：每只标的按 `代码_频率_起止日期` 存 pickle，二次运行直接命中，速度极快。
- **频率聚合**：支持 `1m/5m/15m/30m/1h/1d/1w`，可将分钟数据聚合到日线，反之不行。
- **20 维特征**（本项目核心）：

  | 类别 | 特征 |
  |---|---|
  | 收益 | `open_return` `high_return` `low_return` `close_return` |
  | 量价形态 | `volume_change` `range_ratio` `body_ratio` `upper_shadow` `lower_shadow` |
  | 均线偏离 | `ma5` `ma10` `ma20` `ma60` |
  | 波动 | `std5` `std20` |
  | 技术指标 | `rsi` `macd` `macd_signal` |
  | 横截面 | `correlation`（与市场均值20日相关）`market_rank`（当日市场排名分位） |

- **输出**：`training_data.npy`（标准化后）+ `training_dates.npy` + `data_mean.npy` + `data_std.npy`。
- **跨周期模式**：当 `input_frequency != target_frequency` 时，自动切换为「输入窗口 → 目标频率标签」的配对样本模式（`training_labels.npy` / `training_date_pairs.npy`）。

```bash
uv run python train_data_generator.py                # 常规：同频率
uv run python train_data_generator.py --force-refresh  # 强制重新下载
uv run python train_data_generator.py --window-size 5  # 指定窗口
```

---

### 3. `main.py` — 训练 + 回测主程序

**职责**：训练 Transformer 并立即在测试集上跑仓位控制回测。

- **模型 `DistributionPredictor`**：线性投影 → 位置编码 → 3 层 TransformerEncoder（`d_model=256`、8 头、`dim_feedforward=1024`、GELU、pre-norm）→ 取最后一步 → 输出全维度分布。
- **训练策略**：`AdamW` + `CosineAnnealingLR`（200 步 warmup）→ MSELoss → 梯度裁剪（`max_norm=0.3`）→ 早停（patience=200）→ 支持 AMP 混合精度与梯度累积。数据按 **7 : 1.5 : 1.5** 划分训练/验证/测试。
- **回测引擎 `PositionController`**：
  - 仓位档位 `(0, 0.5, 1.0)`，按预测均值的连续 2 日确认决定；单日最大调仓幅度 30%。
  - 风控：止损 **-5%**、止盈 **+10%**、开仓阈值 **0.05%**、弱信号阈值 **0.01%**。
  - **分布漂移闸门**：JS 散度超过动态阈值（近 20 日漂移 90 分位）时强制空仓。
  - 成本：万分之一；滚动持仓 5 日。
- **绩效输出**：总收益率、夏普比率、最大回撤、交易成本、交易次数。
- **图表输出**：一张 5 宫格图 —— 真实/预测均值、5% VaR 左尾风险、分布漂移 vs 阈值、仓位变化、组合净值。
- **落盘**：`predict_output/black_swan_analysis.csv`（逐日全指标）、`predict_output/portfolio_stats.txt`。
- **兜底**：若无训练数据，会自动生成**模拟行情**跑通流程（仅用于验证代码，不具实盘意义）。

```bash
uv run python main.py                       # 默认 1000 轮
uv run python main.py --amp                 # 开启混合精度（省显存）
uv run python main.py --epochs 300 --lr 1e-5
uv run python main.py --no-early-stop       # 关掉早停
```

> 关键参数：`--epochs` `--batch-size`(128) `--lr`(1e-5) `--gradient-accumulation` `--amp` `--workers`

---

### 4. `predict.py` — 单日预测（实盘决策出口）

**职责**：加载训练好的模型，对指定日期/时点输出一份完整"市场体检报告"。

- 支持预测**指定日期**，也支持**指定时点**（`HH:MM:SS`，用于盘中分钟级；找不到精确时点会自动匹配最近时点并提示偏差）。
- 标的名称来源：优先 QMT → 回落到 `ETF.json`。
- 报告 8 个部分：

  | 序号 | 内容 |
  |---|---|
  | 一 | 全市场统计：预测均值、中位数、标准差、偏度、峰度 |
  | 二 | 风险指标：VaR(1%/5%)、上行分位 VaR(95%/99%)、收益区间 |
  | 三 | 市场情绪：上涨/下跌家数、上涨比例 |
  | 四 | **市场风险评估**：0-100 分打分（收益/波动/尾部/广度/峰度/偏度六项加权），分 LOW/MEDIUM/HIGH |
  | 五 | 表现最好的前 N 个标的 |
  | 六 | 表现最差的前 N 个标的 |
  | 七 | **交易信号**：`STRONG_BUY` / `CAUTIOUS_BUY` / `WEAK_BUY` / `HOLD` / `SELL` / `RISK_OFF` |
  | 八 | 保存结果 |

- **落盘**：`predict_output/prediction_日期.csv`（逐标的预期收益）、`predict_output/summary_日期.txt`。

```bash
uv run python predict.py --date 2026-06-24                    # 预测某日
uv run python predict.py --date 2026-06-24 --time 14:30:00    # 预测盘中某时点
uv run python predict.py --date 2026-06-24 --top-n 20 --bottom-n 20
```

> 支持特殊用法：`--date` 传「数据最后一天的下一天」时，自动用最后 60 天数据推演次日。

---

### 5. `etf_hedge_analysis.py` — ETF 期权对冲分析

**职责**：为「强势 ETF 轮动策略」测算系统性风险对冲方案。

- **标的**：行业 ETF（从 `ETF.json` 读取，按关键词自动归类：科技/医药/消费/金融/周期/新能源/军工/地产/农业/其他）× 宽基 ETF（沪深300 `510300`、上证50 `510050`、中证500 `510500`、科创50 `588000`、创业板 `159915`）。
- **计算**：
  1. 全量 ETF 日收益率 → **相关系数矩阵**；
  2. 每个行业 ETF 对每个宽基做 **滚动 60 日 Beta 回归**（均值 Beta / 最新 Beta / Beta 波动），取相关性最高者作为**最佳对冲工具**；
  3. 相关性 > 0.7 → 强烈推荐对冲；0.4~0.7 → 推荐；< 0.4 → 不建议；
  4. 拉取宽基 ETF 的**认购/认沽期权合约**，定位平值（ATM）合约。
- **对冲仓位公式**：`对冲仓位 = |Beta| × 行业ETF持仓金额 / 期权合约单位`
- **落盘**：`hedge_data/` 下 4 个 CSV（对冲结果、相关矩阵、期权信息、收益率）。

```bash
uv run python etf_hedge_analysis.py --start-date 2025-01-01 --end-date 2026-06-24
uv run python etf_hedge_analysis.py --force-refresh
```

---

### 6. `visualize.py` — Streamlit 可视化面板

**职责**：把预测结果画成一眼能看懂的热力图。

- **主视图**：`treemap` 大盘预测热力图，层级为「行业 → 标的」，**面积 = 预期收益强度，颜色 = 预期收益**（绿降红升，符合 A 股习惯）；顶部三张指标卡：最强收益、最大亏损、多空比。
- **黑天鹅分析区**：4 张 Plotly 折线 —— 真实/预测均值、真实/预测 VaR5%、分布漂移 vs 阈值、仓位曲线。
- 数据源为 `predict_output/` 下的 CSV，日期可选。

```bash
uv run streamlit run visualize.py
```

---

### 7. `pyproject.toml` — 依赖与源

- Python **≥ 3.13**，uv 管理。
- 依赖：`numpy` `matplotlib` `scipy` `torch` `torchvision` `xtquant` `streamlit` `plotly`。
- 源：阿里云 PyPI 镜像（默认） + PyTorch CUDA 128 官方源（`torch`/`torchvision` 单独走 CUDA 源）。

---

## 四、完整使用流程（从零到出结果）

```bash
# 0. 准备环境（uv 会自动建虚拟环境）
uv sync

# 1. 改配置：打开 config.py
#    - 填 QMTConfig.token
#    - 确认 QMTConfig.data_home_dir
#    - 确认 DataConfig 起止日期与 ETF 清单
#    - 若用 QMT 交易端，改 QMTTraderConfig.path / account_id

# 2. 生成训练数据（产出 data/*.npy）
uv run python train_data_generator.py

# 3. 训练模型 + 回测（产出 models/*.pth、predict_output/black_swan_analysis.csv）
uv run python main.py --amp

# 4. 预测某一天（产出 prediction_*.csv / summary_*.txt）
uv run python predict.py --date 2026-06-24

# 5. 可视化（浏览器打开热力图，默认 http://localhost:8501）
uv run streamlit run visualize.py

# 6. 选做：期权对冲分析
uv run python etf_hedge_analysis.py --start-date 2025-01-01
```

**依赖顺序**：`config.py` → `train_data_generator.py` → `main.py` → `predict.py` → `visualize.py`。
（`etf_hedge_analysis.py` 独立，只依赖 `ETF.json` 与 QMT。）

---

## 五、重点注意事项

**必须先处理的配置项**
1. `QMTConfig.token` 为空，**必须填写**自己的 QMT 数据 token，否则无法连数据中心。
2. `QMTTraderConfig.account_id` 与路径为硬编码示例值，实盘前务必替换。
3. `ETF.json`（标的中文名映射）当前**缺失**，不影响运行，但名称列会退化为显示代码；建议补一份 `{ "512170.SH": "医疗ETF", ... }`。
4. 首次运行前无 `data/`、`models/`、`predict_output/`，需按流程逐步生成。

**数据源降级逻辑**
- QMT 不可用 → 自动读 `data/etf_hist_data.csv`（需列：`symbol, timestamp, open, high, low, close, volume`）。
- 训练数据也缺失 → `main.py` 用**模拟数据**跑通流程（仅验证代码，无实盘价值）。

**性能提示**
- 输入维度 = `63 只 × 20 特征 = 1260`，窗口 60 → 模型输入 `60 × 1260`。8GB 显存建议开 `--amp` 并调小 `--batch-size`。
- `--workers` 在 Windows 上建议保持 0，避免 DataLoader 多进程开销。
- 缓存目录 `cache/` 可安全删除，删后会重新下载。

**安全提示** ⚠️
- `config.py` 中明文存放了 **QMT 账户 ID** 与服务器 IP。若本项目要上传 GitHub / 共享，**请务必先脱敏**（改为读环境变量或占位符）。

---

## 六、常见问题速查

| 现象 | 原因 / 解决 |
|---|---|
| `QMT模块导入失败` | 未安装 `xtquant`（需在 QMT 环境），系统会自动降级本地 CSV |
| `训练数据不存在: data/training_data.npy` | 先跑 `train_data_generator.py` |
| `模型文件不存在: models/distribution_predictor.pth` | 先跑 `main.py` |
| `配置股票池数量与训练数据数量不一致` | `config.py` 的 ETF 清单与生成数据时的清单不同，改完需重新生成数据 |
| 预测日期不在训练数据中 | 用数据范围内的日期，或用最后一天的下一天做次日推演 |
| Streamlit 提示输出目录不存在 | 先跑 `predict.py` 生成预测 CSV |
| 可视化里没有黑天鹅图 | 需先跑过 `main.py` 生成 `black_swan_analysis.csv` |

---

## 七、策略逻辑小结（便于快速理解）

| 指标 | 含义 | 触发动作 |
|---|---|---|
| 预测均值 > 0.05%（连续 2 日） | 全市场普遍上行 | 满仓（100%） |
| 预测均值 > 0.01%（连续 2 日） | 弱上行 | 半仓（50%） |
| 预测均值 ≤ 0.01% | 震荡/走弱 | 空仓 |
| JS 漂移 > 动态阈值 | 市场结构突变（黑天鹅前兆） | 强制空仓 |
| 单笔亏损 -5% / 盈利 +10% | 止损 / 止盈 | 平仓 |
| 风险评分 ≥ 70 / 40 | 高风险 / 中风险 | 输出 `RISK_OFF` 信号 |

---

*说明文件生成于 2026-09-23，基于源码逐行阅读整理。*
